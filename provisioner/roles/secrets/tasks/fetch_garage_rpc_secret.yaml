---
- name: Fetch Garage RPC secret
  block:
    - name: Get Garage RPC secret
      onepassword.connect.field_info:
        vault: "{{ vault_id }}"
        item: "garage"
        section: "rpc"
        field: "secret"
      register: _garage_rpc_secret_raw
      no_log: true
      delegate_to: localhost

    - name: Cache Garage RPC secret
      set_fact:
        secret_garage_rpc_secret: "{{ _garage_rpc_secret_raw.field.value }}"
        cacheable: true
      no_log: true
  run_once: true
  when: secret_garage_rpc_secret is not defined
  become: false
