---
- name: Fetch cloudflare API token
  block:
    - name: Get cloudflare token
      onepassword.connect.field_info:
        vault: "{{ vault_id }}"
        item: "cert-bot"
        field: "cloudflare_api_token"
      register: _cloudflare_token_raw
      no_log: true
      delegate_to: localhost

    - name: Cache cloudflare token
      set_fact:
        secret_cloudflare_token: "{{ _cloudflare_token_raw.field.value }}"
        cacheable: true
      no_log: true
  run_once: true
  when: secret_cloudflare_token is not defined
  become: false
