---
##################################
# Storage configuration
# 現時点で用途は未定。パーティションの切り直しには再フラッシュが必要なため、
# 先に ext4 データ領域だけ確保しておく。
##################################
storage_mode: ext4
mount_point: /storage
