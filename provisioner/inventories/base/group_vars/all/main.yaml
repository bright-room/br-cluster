---
##################################
# Ansible configuration
##################################
ansible_user: bradmin

##################################
# System configuration
##################################
timezone: "Asia/Tokyo"
tmp_dir: "/tmp"
install_dir: "/usr/local/bin"
gpu_memory_mb: 16

##################################
# Storage configuration
# storage_mode:
#   none - OS ディスク全体を root に使う (gateway1, node1-3)
#   ext4 - OS 用 + データ用パーティションに分割 (worker nodes: node4-7)
##################################
storage_mode: none
os_partition_size_mib: 65536  # 64 GiB
mount_opts: "defaults,noatime,discard,nofail"
