apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tempo

configMapGenerator:
  - name: tempo-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
