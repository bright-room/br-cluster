---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns-coredns

configMapGenerator:
  - name: external-dns-coredns-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
