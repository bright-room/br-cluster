---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: alloy

configMapGenerator:
  - name: alloy-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
