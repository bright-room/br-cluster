---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm.yaml
  - externalsecret-archive.yaml
  - externalsecret-artifact.yaml
  - externalsecret-sso.yaml
  - externalsecret-discord.yaml
  - eventbus.yaml

configMapGenerator:
  - name: argo-workflows-helm-values
    files:
      - base-values.yaml=values-workflows.yaml
  - name: argo-events-helm-values
    files:
      - base-values.yaml=values-events.yaml
configurations:
  - kustomizeconfig.yaml
