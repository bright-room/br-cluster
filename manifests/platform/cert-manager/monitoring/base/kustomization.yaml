---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager

configMapGenerator:
  - name: cert-manager-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
