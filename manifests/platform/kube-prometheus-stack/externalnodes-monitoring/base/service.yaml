apiVersion: v1
kind: Service
metadata:
  name: external-nodes
  labels:
    app: external-node-exporter
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
      protocol: TCP
