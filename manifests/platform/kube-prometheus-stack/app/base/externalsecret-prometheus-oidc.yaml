apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prometheus-oidc
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-backend
    kind: ClusterSecretStore
  target:
    name: prometheus-oidc
    template:
      data:
        client-id: "{{ .client_id }}"
        client-secret: "{{ .client_secret }}"
  data:
    - secretKey: client_id
      remoteRef:
        key: tf-zitadel-output
        property: prometheus_client_id
    - secretKey: client_secret
      remoteRef:
        key: tf-zitadel-output
        property: prometheus_client_secret
