---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: alloy-cp

configMapGenerator:
  - name: alloy-cp-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
