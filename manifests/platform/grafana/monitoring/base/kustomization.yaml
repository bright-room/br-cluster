---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana

configMapGenerator:
  - name: grafana-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
