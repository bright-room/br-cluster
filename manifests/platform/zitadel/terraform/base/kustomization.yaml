---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No top-level namespace: gitrepository.yaml targets flux-system while the
# rest live in zitadel. Each resource declares its own namespace.
resources:
  - gitrepository.yaml
  - tf-runner.yaml
  - terraform.yaml
