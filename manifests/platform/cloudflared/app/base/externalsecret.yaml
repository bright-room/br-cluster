apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflared-tunnel
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: cloudflared-tunnel-credentials
    template:
      engineVersion: v2
      data:
        credentials.json: |
          {"AccountTag":"{{ .account_tag }}","TunnelSecret":"{{ .tunnel_secret }}","TunnelID":"{{ .tunnel_id }}"}
  data:
    - secretKey: account_tag
      remoteRef:
        key: cloudflared
        property: cluster_internal_account_tag
    - secretKey: tunnel_secret
      remoteRef:
        key: cloudflared
        property: cluster_internal_tunnel_secret
    - secretKey: tunnel_id
      remoteRef:
        key: cloudflared
        property: cluster_internal_tunnel_id
