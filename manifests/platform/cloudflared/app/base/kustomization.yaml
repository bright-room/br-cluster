apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # 旧 tunnel (br-cluster-platform) — 移行完了まで維持
  - externalsecret.yaml
  - configmap.yaml
  - deployment.yaml
  # 新 tunnel (br-cluster)
  - externalsecret-br-cluster.yaml
  - configmap-br-cluster.yaml
  - deployment-br-cluster.yaml
