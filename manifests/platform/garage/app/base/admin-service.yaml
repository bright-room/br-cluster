apiVersion: v1
kind: Service
metadata:
  name: garage-admin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: garage
    app.kubernetes.io/instance: garage
  ports:
    - name: admin
      port: 3903
      targetPort: 3903
      protocol: TCP
