apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm.yaml
  - admin-externalsecret.yaml
  - garage-externalsecret.yaml
  - admin-service.yaml

configMapGenerator:
  - name: garage-helm-values
    files:
      - base-values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
