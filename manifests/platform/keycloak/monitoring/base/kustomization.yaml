apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: keycloak

configMapGenerator:
  - name: keycloak-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
