apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: loki

configMapGenerator:
  - name: loki-monitoring-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
